name: Release
env:
  CI: true
  FORCE_COLOR: 1
on:
  push:
    branches:
      - master
jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      paths_released: ${{ steps.release.outputs.paths_released }}
      releases_created: ${{ steps.release.outputs.releases_created }}
      # 'packages/wallet--release_created': ${{ steps.release.outputs['packages/wallet--release_created'] }}
      # 'packages/access--release_created': ${{ steps.release.outputs['packages/access--release_created'] }}
      # 'packages/access-api--release_created': ${{ steps.release.outputs['packages/access-api--release_created'] }}
    steps:
      - uses: GoogleCloudPlatform/release-please-action@v3
        id: release
        with:
          command: manifest
          config-file: .github/release-please-config.json
          manifest-file: .github/release-please-manifest.json
  publish:
    needs: release
    if: contains(needs.release.outputs.paths_released, 'packages/access')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: pnpm/action-setup@v2.0.1
        with:
          version: 7
      - uses: actions/setup-node@v2
        with:
          node-version: 18
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm -r build
      - run: echo 'npm'
      # - run: pnpm -r publish --tag next --access public
      #   env:
      #     NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}
  cf-pages:
    needs: release
    if: contains(needs.release.outputs.paths_released, 'packages/wallet')
    runs-on: ubuntu-latest
    steps:
      - run: echo 'pages'
  cf-worker:
    needs: release
    if: contains(needs.release.outputs.paths_released, 'packages/access-api')
    runs-on: ubuntu-latest
    steps:
      - run: echo 'worker'
