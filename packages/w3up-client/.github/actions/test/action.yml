name: Test
description: 'test'
# install npm dependencies (e.g. via `npm` or monorepo `pnpm`) before invoking this action to run tests
runs:
  using: "composite"
  steps:
    - run: npm run lint
      shell: bash
    - run: npm test
      shell: bash
